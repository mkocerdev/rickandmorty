query episode($episodeId: ID!) {
 episode(id: $episodeId) {
    id,
      name,
      air_date,
      episode,
      characters {
        id,
        name
        status,
        species,
        type,
        gender,
        image,
        created,
        origin {
        name,
        },
      }
  }
}
