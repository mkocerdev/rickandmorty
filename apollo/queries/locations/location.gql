query location($locationId: ID!) {
 location(id: $locationId) {
    id,
    name,
    type,
    dimension,
    residents {
      id,
      name
      status,
      species,
      gender,
      image,
      origin {
        name,
      },
    }
  }
}
